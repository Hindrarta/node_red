version: "3.2"

services:
  red:
    image: nodered/node-red
    container_name: hindrarta-node-red-1.0
    ports:
      - 1880:1880
    volumes:
      - ./data:/data
    #   - ./public:/home/nol/node-red-static
    # links:
      # - mongodb
      # - mqtt
  mqtt:
    image: eclipse-mosquitto:1.6.12
    container_name: hindrarta-mqtt-broker-1.0
    ports:
      - 1883:1883
  # mongodb:
  #   ports:
  #     - 27017:27017
  #   image: arm64v8/mongo:4
  #   container_name: mongodb
  #   volumes:
  #     - ./db:/data/db
  #   restart: unless-stopped

# networks:
#   app-networks:
#     external:
#       name: nodered_default